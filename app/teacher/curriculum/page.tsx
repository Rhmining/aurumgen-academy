import { TeacherShell } from "@/components/teacher/teacher-shell";

export default function TeacherCurriculumPage() {
  return (
    <TeacherShell title="Curriculum Planner" description="Rancang scope, sequence, dan target mastery setiap cohort.">
      <section className="surface rounded-[1.75rem] p-6">
        <p className="text-[rgb(var(--muted))]">Halaman ini siap dikembangkan menjadi editor roadmap kurikulum per pathway.</p>
      </section>
    </TeacherShell>
  );
}
